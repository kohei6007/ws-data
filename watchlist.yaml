# 監視リスト（このファイルを編集すると次回 GitHub Actions 実行時に反映される）
# スマホからもGitHub web UIで直接編集できる

tickers:
  # 日本株（証券コード4桁）
  - code: "8035"
    name: "東京エレクトロン"
    note: "半導体製造装置（エッチング/成膜）"

  - code: "6920"
    name: "レーザーテック"
    note: "EUVマスク検査、AI/先端半導体"

  # 追加例
  # - code: "6857"
  #   name: "アドバンテスト"
  #   note: "半導体テスタ・HBM"

# Cron 実行のオン/オフ（オフにするとAPIコール節約）
enabled: true

# 出力する snapshot.json に含める内容
options:
  with_chart: false  # 画像は重いのでデフォルトOFF。必要時 true
  market_only: false
